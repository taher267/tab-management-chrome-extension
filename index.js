window.addEventListener("DOMContentLoaded", function () {
    // const button = `<button class="button" id="openPopup" >Start Scraping</button>`;
    // window.document.body.insertAdjacentHTML("afterend", button);
});